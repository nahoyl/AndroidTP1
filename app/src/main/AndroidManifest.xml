<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iutmontpellier.dallecortb.tp1ex3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AffichageNomPrenom"
            android:label="@string/title_activity_affichage_nom_prenom" />
        <activity android:name=".ImageScrollerActivity" />
        <activity
            android:name="com.iutmontpellier.dallecortb.photos.PhotoFullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen_picture"
            android:theme="@style/FullscreenTheme"></activity>
    </application>

</manifest>